import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.izquierdo.starter',
  appName: 'izquierdo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
